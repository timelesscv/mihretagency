rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow read/write to Candidates and Settings collections
    match /candidates/{passport} {
      allow read, write: if true;
    }

    match /settings/{settingId} {
      allow read, write: if true;
    }
  }
}
